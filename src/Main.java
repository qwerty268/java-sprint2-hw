import tracker.service.InMemoryTasksManager;

public class Main {
    public static void main(String[] args) {
        InMemoryTasksManager manager = new InMemoryTasksManager();
    //никаких тестов нет(( хз как их писать
    }
}
